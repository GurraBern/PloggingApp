<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    x:Class="PlogPal.Maui.Features.PloggingSession.AddLitter"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:ploggingsession="clr-namespace:PlogPal.Maui.Features.PloggingSession"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    xmlns:viewmodel="clr-namespace:PlogPal.Maui.Features.PloggingSession"
    xmlns:DomainEnums="clr-namespace:PlogPal.Domain.Enums;assembly=PlogPal.Domain"
    x:DataType="viewmodel:AddLitterViewModel"> 

    <Border HeightRequest="250" Stroke="Transparent">
        <Grid IsVisible="{Binding IsTracking}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <ImageButton
                x:Name="expandBtn"
                Grid.Row="1"
                Grid.Column="0"
                Clicked="ShowMoreLitterButtons"
                HeightRequest="40"
                Source="line_angle_left_icon.png"
                WidthRequest="40">
                <ImageButton.Behaviors>
                    <toolkit:IconTintColorBehavior TintColor="{StaticResource OffBlack}"/>
                </ImageButton.Behaviors>
            </ImageButton>

            
            <ploggingsession:LitterTypeButton
                Grid.Row="0"
                Grid.Column="1"
                BackgroundColor="#8865f1"
                Command="{Binding AddLitterCommand}"
                CommandParameter="{x:Static DomainEnums:LitterType.Plastics}"
                HeightRequest="80"
                HorizontalOptions="End"
                ImageSource="candy_icon.png"
                Text="plastics(s)"
                WidthRequest="80"/>
            <ploggingsession:LitterTypeButton
                Grid.Row="1"
                Grid.Column="1"
                BackgroundColor="#a254d5"
                Command="{Binding AddLitterCommand}"
                CommandParameter="{x:Static DomainEnums:LitterType.SmallMetal}"
                HeightRequest="80"
                HorizontalOptions="End"
                ImageSource="soda_cans_icon.png"
                Text="metal"
                WidthRequest="80"/>
            <ploggingsession:LitterTypeButton
                Grid.Row="2"
                Grid.Column="1"
                BackgroundColor="#b346b1"
                Command="{Binding AddLitterCommand}"
                CommandParameter="{x:Static DomainEnums:LitterType.Cigarette}"
                HeightRequest="80"
                HorizontalOptions="End"
                ImageSource="smoking_icon.png"
                Text="cigarette"
                WidthRequest="80"/>
            <ploggingsession:LitterTypeButton
                Grid.Row="0"
                Grid.Column="2"
                BackgroundColor="#bd3b8a"
                Command="{Binding AddLitterCommand}"
                CommandParameter="{x:Static DomainEnums:LitterType.LargePlastics}"
                HeightRequest="80"
                HorizontalOptions="End"
                ImageSource="pet_bottle_icon.png"
                Text="plastics(l)"
                WidthRequest="80"/>
            <ploggingsession:LitterTypeButton
                Grid.Row="1"
                Grid.Column="2"
                BackgroundColor="#c03460"
                Command="{Binding AddLitterCommand}"
                CommandParameter="{x:Static DomainEnums:LitterType.Glass}"
                HeightRequest="80"
                HorizontalOptions="End"
                ImageSource="broken_glass_icon.png"
                Text="glass"
                WidthRequest="80"/>
            <ploggingsession:LitterTypeButton
                Grid.Row="2"
                Grid.Column="2"
                BackgroundColor="#bc3434"
                Command="{Binding AddLitterCommand}"
                CommandParameter="{x:Static DomainEnums:LitterType.Snus}"
                HeightRequest="80"
                HorizontalOptions="End"
                ImageSource="snus_icon.png"
                Text="snus"
                WidthRequest="80"/>
        </Grid>
    </Border>
</ContentView>
