<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PloggingApp.MVVM.Views.MyProfileView"
             xmlns:modelsCore="clr-namespace:Plogging.Core.Models;assembly=Plogging.Core"
             xmlns:models="clr-namespace:PloggingApp.MVVM.Models"
             xmlns:components="clr-namespace:PloggingApp.MVVM.Views.Components"
             xmlns:views="clr-namespace:PloggingApp.MVVM.Views"
             xmlns:viewmodel="clr-namespace:PloggingApp.MVVM.ViewModels"
             xmlns:converters="clr-namespace:PloggingApp.Converters"
             x:DataType="viewmodel:MyProfileViewModel">

    <!-- TODO: Add navigation to stats/history page? -->

    <ContentView.Resources>
        <converters:FirstTwoCharsConverter x:Key="FirstTwoCharsConverter" />
        <converters:TitleCaseConverter x:Key="TitleCaseConverter"/>
    </ContentView.Resources>

    <ScrollView>
        <VerticalStackLayout Margin="5" >
            <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}"/>
            <Border Style="{StaticResource RoundBorder}" StrokeShape="RoundRectangle 90, 90, 0, 0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="90"/>
                        <RowDefinition Height="30"/>
                    </Grid.RowDefinitions>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Ellipse Grid.Row="0" Grid.Column="0" Stroke="Black" StrokeThickness="2" WidthRequest="70" HeightRequest="70"></Ellipse>
                    <Label Grid.Row="0" Grid.Column="0" 
                       Text="{Binding DisplayName, Converter={StaticResource FirstTwoCharsConverter}}" 
                       TextColor="Black" 
                       Style="{StaticResource SubHeadline}"
                       HorizontalOptions="Center"
                       VerticalOptions="Center"/>

                    <Label Grid.Row="0" Grid.Column="1" Text="{Binding DisplayName}" Style="{StaticResource SubHeadline}"  BackgroundColor="White" VerticalOptions="Center"/>
                    <views:StreakView Grid.Row="0" Grid.Column="2" BindingContext="{Binding StreakViewModel}" HorizontalOptions="Center" VerticalOptions="Center"/>

                    <Label Grid.Row="1" Grid.Column="0" Text="{Binding UserRankInt, StringFormat='Rank: {0}'}" BackgroundColor="White" HorizontalOptions="Center"/>
                </Grid>


            </Border>

            <Border Style="{StaticResource RoundBorder}">
                <StackLayout Margin="0,10" Orientation="Vertical" HeightRequest="100">
                    <Label Text="This Month" FontSize="Medium" HorizontalOptions="Start" Margin="10,5"/>
                    <Grid Margin="10,5">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100" />
                                <ColumnDefinition Width="100" />
                                <ColumnDefinition Width="120" />
                            </Grid.ColumnDefinitions>
                            <StackLayout Grid.Column="0" Orientation="Vertical">
                                <Label Text="Litter collected" VerticalOptions="Center"/>
                                <Label Text="{Binding TotalWeight, StringFormat='{0}kg'}" />
                            </StackLayout>
                            <StackLayout Grid.Column="1" Orientation="Vertical" Margin="20,0">
                                <Label Text="Distance" VerticalOptions="Center"/>
                                <Label Text="{Binding TotalDistance, StringFormat='{0}m'}" />
                            </StackLayout>
                            <StackLayout Grid.Column="2" Orientation="Vertical" Margin="20,0">
                                <Label Text="Saved CO2" VerticalOptions="Center"/>
                                <Label Text="{Binding TotalCO2Saved, StringFormat='{0}CO2'}"/>
                            </StackLayout>
                        </Grid>
                    </Grid>
                </StackLayout>
            </Border>

            <Border Style="{StaticResource RoundBorder}">
                <StackLayout Margin="0,10" Orientation="Vertical">
                    <Label Text="History" Margin="10,5" FontSize="Medium" HorizontalOptions="Start"/>

                    <CollectionView ItemsSource="{Binding LatestSessions}" ItemsLayout="HorizontalList">
                        <CollectionView.ItemTemplate>
                            <DataTemplate x:DataType="modelsCore:PloggingSession">
                                <Grid Margin="0,10">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="65" />
                                        <ColumnDefinition Width="45" />
                                    </Grid.ColumnDefinitions>

                                    <Image Source="{Binding Image}" >
                                        <Image.Clip>
                                            <EllipseGeometry RadiusX="28" RadiusY="28" Center="32,40"/>
                                        </Image.Clip>
                                    </Image>

                                    <Label Grid.Row="0" Grid.Column="1" Text="{Binding PloggingData.Weight, StringFormat='{0:F1}kg'}" HorizontalOptions="Center" VerticalOptions="Start"/>
                                    <Image Grid.Row="0" Grid.Column="1" Source="trashbag.png" WidthRequest="20" HeightRequest="20" VerticalOptions="End"/>
                                    <Label Grid.Row="1" Grid.ColumnSpan="2" Text="{Binding StartDate, StringFormat='{0:dd MMMM}'}" HorizontalOptions="Center"/>
                                </Grid>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </StackLayout>
            </Border>



            <Border Style="{StaticResource RoundBorder}" StrokeShape="RoundRectangle 0, 0, 50, 50" >
                <StackLayout Margin="10">
                    <Label Text="Badge Collection" FontSize="Medium" Margin="10, 5" HorizontalOptions="Start"/>

                    <Grid Margin="0,10,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="16*"/>
                        </Grid.ColumnDefinitions>
                        <BoxView Grid.Column="0" BackgroundColor="White"/>

                        <views:BadgesView BindingContext="{Binding BadgesViewModel}" 
                            HorizontalOptions="Start" 
                            VerticalOptions="StartAndExpand" 
                            Scale="1.4"
                            Grid.Column="1">
                        </views:BadgesView>
                    </Grid>
                </StackLayout>
            </Border>




        </VerticalStackLayout>
    </ScrollView>
</ContentView>


